# MLflow boundary

MLflow stores versioned model and evaluation metadata only. It is not an authority
for policy, case, action, financial, or terminal state. Reports carry replay/live
labels, dataset provenance, split metadata, and confidence-interval references.

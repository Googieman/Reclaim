# LightGBM attribution baseline fixture

`baseline-v1.0.0.json` is a small replay-labeled development fixture for adapter
parity tests. It records feature inputs, expected advisory outputs, timeline and
evidence references, and model/version provenance.

This fixture is not a trained-model quality benchmark and contains no production
performance claim. It must remain suitable for deterministic unit tests without
provider credentials or network access.

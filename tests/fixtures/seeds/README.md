# Deterministic seed policy

Seeds are stable identifiers for reproducible simulator and replay variants. Store only
development and validation seeds here. Held-out seeds and scenarios are managed outside
the repository and are inaccessible to prompts, tuning, and model selection.

# Dataset sources

Only approved fixture/source manifests belong here. Do not copy sealed held-out
payloads or secrets into this directory. The builder receives source paths explicitly,
records their checksums, and emits only development/validation rows permitted by the
manifest policy.
